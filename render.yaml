# Render Blueprint for Kev-Clip
services:
  - type: web
    name: kev-clip
    runtime: node
    nodeVersion: 20
    repo: https://github.com/KevPH2026/kev-clip
    branch: main
    buildCommand: npm install
    startCommand: npm start
    plan: free
    healthCheckPath: /api/health